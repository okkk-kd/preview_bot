CREATE TABLE bot (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    phone TEXT NOT NULL,
    nickname TEXT NOT NULL,
    tg_id TEXT NOT NULL,
    status INTEGER NOT NULL,
    tariff TEXT NOT NULL
);